# 项目级上下文

项目级上下文记录具体项目、模块和服务的知识。模板提供 `example/` 作为结构示例，真实项目初始化后应替换或扩展它。

推荐结构：

```text
context/project/
  example/
    INDEX.md
    experience/
```

项目级内容适合沉淀：

- 架构、模块边界和关键数据流。
- 外部依赖、运行手册和排障路径。
- Agent 或工程师反复踩到的模式性经验。
